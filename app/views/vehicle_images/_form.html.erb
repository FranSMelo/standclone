<%= form_with(model: vehicle_image) do |form| %>
  <% if vehicle_image.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vehicle_image.errors.count, "error") %> prohibited this vehicle_image from being saved:</h2>

      <ul>
        <% vehicle_image.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :vehicle_id, style: "display: block" %>
    <%= form.text_field :vehicle_id %>
  </div>

  <div>
    <%= form.label :image_path, style: "display: block" %>
    <%= form.text_field :image_path %>
  </div>

  <div>
    <%= form.label :primary_image, style: "display: block" %>
    <%= form.checkbox :primary_image %>
  </div>

  <div>
    <%= form.label :position, style: "display: block" %>
    <%= form.number_field :position %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
